# 23A-2 Tests, Invariants, Fixtures, Demos, and Validation Evidence Inventory

## 1. Inspection baseline and scope

- Phase: Tailtriage review phase 23A-2.
- Base verification: `git status --short` produced no output; `git rev-parse HEAD` produced `d7c8b0f756ac8f8dde5b7ce3b243510e6520e179`.
- Scope: evidence-only inventory of tests, invariants, fixtures, demos/workloads, validation scripts, generated-output ownership, repeated proof surfaces, and gaps.
- Exclusions: no production code, tests, fixtures, demos, examples, manifests, scripts, workflows, dependencies, generated artifacts, or public docs were changed.
- Discovery outputs were written under `/tmp`: `/tmp/23a2-cargo-metadata.json`, `/tmp/23a2-test-list-rerun.txt`, `/tmp/23a2-files.txt`, `/tmp/23a2-rg.txt`, `/tmp/23a2-sizes.txt`.
- `cargo test --workspace --all-features -- --list > /tmp/23a2-test-list-rerun.txt 2>&1` exited 0 after compiling/listing test targets; it did not execute test bodies.

## 2. Test-target and proof map

### Workspace targets from Cargo metadata

| Package | Path | Test target or module | Feature/platform | Command | Primary behavior or invariant | Inputs/fixtures | Proof class | Uncertainty |
|---|---|---|---|---|---|---|---|---|
| `tailtriage-core` | `tailtriage-core/src/lib.rs::tests`; `tailtriage-core/src/tests.rs`; module inline tests in `collector`, `sink`, and `time` | inline unit tests | all features | `cargo test -p tailtriage-core --all-features --lib` | Capture lifecycle, generated and explicit request IDs, schema v2/finalization shape, capture retention and truncation flags, shutdown semantics, strict validation and permissive normalization, sink/finalization failure behavior. | synthetic Runs, in-memory sinks, local temp sinks | primary core proof | External persisted Run populations remain unknown. |
| `tailtriage-analyzer` | `tailtriage-analyzer/src/lib.rs` and module files | inline unit modules: attribution, stage attribution, options, scoring, slicing, temporal, route, top-level analyzer tests | all features; no platform conditions observed | `cargo test -p tailtriage-analyzer --all-features --lib` | Analyzer scoring, confidence, ordering, warnings, rendering, options, strict/permissive Run handling, arithmetic boundaries, deterministic order. | mostly synthetic builders in source; `examples/analyzer-config.toml` for TOML example parsing | primary proof | Large top-level test module mixes many invariant families; grouping is by module/name prefix rather than every function. |
| `tailtriage-analyzer` | `tailtriage-analyzer/tests/analyzer_fixtures.rs` | integration fixture contract | all features | `cargo test -p tailtriage-analyzer --test analyzer_fixtures` | Fixture Runs render to expected report JSON. | `tailtriage-analyzer/tests/fixtures/*.json`; `tailtriage-analyzer/tests/expected/*.report.json` | primary fixture/golden proof | Producer for the committed fixture Runs is not established by file inspection. |
| `tailtriage-analyzer` | `tailtriage-analyzer/tests/report_schema_contract.rs` | report serialization schema contract | all features | `cargo test -p tailtriage-analyzer --test report_schema_contract` | Public report JSON includes/omits expected fields and remains parseable. | synthetic Runs in test | primary serialization/rendering proof | Does not enumerate all report shapes. |
| `tailtriage-analyzer` | `tailtriage-analyzer/tests/public_api_contract.rs` | public API compile/behavior contract | all features | `cargo test -p tailtriage-analyzer --test public_api_contract` | Public analyzer functions and option APIs remain usable from integration-test context. | synthetic Runs | primary public API proof | External downstream compile combinations remain unknown. |
| `tailtriage-analyzer` | `tailtriage-analyzer/tests/boundary_thresholds.rs` | boundary/permutation thresholds | all features | `cargo test -p tailtriage-analyzer --test boundary_thresholds` | Threshold edges do not drift for queue/executor/blocking/downstream classifications. | synthetic Runs | primary boundary proof | Search did not establish a generated exhaustive table owner. |
| `tailtriage-analyzer` | `tailtriage-analyzer/tests/end_to_end_capture_analysis.rs` | capture-to-analysis integration | all features | `cargo test -p tailtriage-analyzer --test end_to_end_capture_analysis` | Core capture artifacts can be analyzed end-to-end. | synthetic capture in test | primary integration proof | Does not run live demo binaries. |
| `tailtriage-tracing` | `tailtriage-tracing/src/jsonl.rs` | inline JSONL tests | all features | `cargo test -p tailtriage-tracing --all-features --lib jsonl::tests` | Stable JSONL import/export parsing, strict rejection, permissive normalization, errors. | inline JSONL strings | primary tracing serialization proof | File-level grouping only; exact coverage of all malformed JSONL classes is bounded by listed tests. |
| `tailtriage-tracing` | `tailtriage-tracing/src/recorder.rs` | large inline recorder/session tests including async tests | all features; Tokio current-thread async tests present | `cargo test -p tailtriage-tracing --all-features --lib recorder::tests` | Live recorder lifecycle, queue/stage spans, request completion, truncation/cap behavior, runtime snapshots, stable ordering, safety around partial data. | synthetic recorder events | primary capture/lifecycle proof | Dense module contains many overlapping lifecycle subcases; inventory groups by module. |
| `tailtriage-tracing` | `tailtriage-tracing/tests/equivalence.rs` | native-vs-tracing equivalence fixtures | all features | `cargo test -p tailtriage-tracing --test equivalence --all-features` | Stable tracing JSONL imports produce expected Run/report outputs for equivalence scenarios. | `tailtriage-tracing/tests/fixtures/equivalence/*.jsonl`; `native_runs.json`; `tests/expected/equivalence/*.json` | primary equivalence/golden proof | Fixture producer/regeneration command not found. |
| `tailtriage-tracing` | `tailtriage-tracing/tests/equivalence_contract.rs` | equivalence fixture completeness contract | all features | `cargo test -p tailtriage-tracing --test equivalence_contract --all-features` | Equivalence fixtures and expected outputs remain paired. | same equivalence fixture/expected directories | secondary drift proof | Does not establish semantic reason for every fixture. |
| `tailtriage-tracing` | `tailtriage-tracing/tests/live_api_surface.rs`, `live_api_defaults.rs`, `tokio_session.rs` | live API and Tokio integration tests | all features; async tests present | `cargo test -p tailtriage-tracing --all-features --test live_api_surface --test live_api_defaults --test tokio_session` | Builder/session/layer/snapshot/shutdown APIs work and defaults remain stable. | synthetic live sessions | primary API/lifecycle proof | Platform timing sensitivity exists for async runtime tests; current-thread tests are used where noted. |
| `tailtriage-tracing` | `tailtriage-tracing/tests/tracing_examples.rs` | example-based proof surface | all features | `cargo test -p tailtriage-tracing --all-features --test tracing_examples` | Example files compile/run enough to emit parseable artifacts. | `tailtriage-tracing/examples/*.rs`, `examples/tracing_spans.jsonl` | secondary example proof | Expected stdout/artifact exactness is not golden-asserted for every example. |
| `tailtriage-cli` | `tailtriage-cli/src/lib.rs`, `src/artifact.rs` | inline CLI/artifact tests | all features | `cargo test -p tailtriage-cli --all-features --lib` | CLI helpers and artifact naming/path policy. | temp dirs/synthetic names | primary CLI policy proof | Does not exercise subprocess binary behavior directly for every CLI flag. |
| `tailtriage-cli` | `tailtriage-cli/tests/cli_boundary.rs` | integration boundary and command policy tests | all features | `cargo test -p tailtriage-cli --all-features --test cli_boundary` | CLI config, artifact, strict/permissive, output, errors, override policy. | temp files and analyzer fixtures where referenced | primary CLI proof | Uses integration test harness rather than installed binary. |
| `tailtriage-cli` | `tailtriage-cli/tests/json_parity.rs` | JSON parity/serialization tests | all features | `cargo test -p tailtriage-cli --all-features --test json_parity` | CLI JSON outputs match analyzer/library rendering expectations. | synthetic and fixture Runs | primary parity proof | Exact fixture/generator ownership unclear. |
| `tailtriage-cli` | `tailtriage-cli/tests/demo_smoke_fixtures.rs` | demo fixture smoke tests | all features | `cargo test -p tailtriage-cli --all-features --test demo_smoke_fixtures` | Committed demo analysis JSON is parseable and has expected suspect/report properties. | `demos/*/fixtures/*.json` | secondary demo fixture proof | Smoke checks do not prove demo binaries regenerate the fixtures. |
| `tailtriage` | `tailtriage/src/lib.rs`; `tailtriage/tests/tracing_facade.rs` | facade compile-surface tests | feature-gated facade; all features in discovery | `cargo test -p tailtriage --all-features` | Namespace reexports for core/analyzer/controller/tokio/axum/tracing compile and selected tracing APIs are reachable. | none | primary facade compile proof | Does not prove every optional feature combination unpublished consumers use. |
| `tailtriage-axum` | `tailtriage-axum/src/lib.rs::tests` | inline unit tests | all features | `cargo test -p tailtriage-axum --all-features --lib` | Crate identity and default HTTP status-to-outcome mapping. | synthetic HTTP status mapping checks | primary adapter mapping proof | Does not exercise a full Axum service. |
| `tailtriage-axum` | `tailtriage-axum/tests/axum_adapter.rs`; examples | Axum adapter integration/example compile | all features | `cargo test -p tailtriage-axum --all-features` | Axum request integration compiles/behaves in compact examples. | synthetic Axum requests | primary adapter integration proof | No service-level load/asserted diagnosis. |
| `tailtriage-controller` | `tailtriage-controller/src/lib.rs::tests` | inline unit and async tests | all features | `cargo test -p tailtriage-controller --all-features --lib` | Controller enable/disable/reload lifecycle, one-active-generation behavior, finalization schema, sampler lifecycle, request-generation binding, strict lifecycle failure retry, terminal sink failures, TOML parsing and transactional reloads. | temp sinks, synthetic controller generations, TOML snippets | primary controller lifecycle proof | Does not run a long-lived external process. |
| `tailtriage-controller` | `tailtriage-controller/examples/*.rs` | controller examples compile under tests | all features | `cargo test -p tailtriage-controller --all-features --examples` | Controller minimal/TOML startup examples compile. | `tailtriage-controller/examples/*.rs` | secondary example compile proof | Example compile proof is separate from inline lifecycle tests. |
| `tailtriage-tokio` | `tailtriage-tokio/src/lib.rs::{tests, helper_tests}` plus partial-event Tokio tests | inline unit and async tests | all features | `cargo test -p tailtriage-tokio --all-features --lib` | Runtime sampling start/stop/cadence/retention/error behavior, runtime worker counts, mode inheritance, helper queue/stage/inflight behavior, lock/semaphore/channel helpers, partial queue/stage events. | synthetic Tokio runtimes and request handles | primary Tokio runtime/helper proof | Timing-sensitive behavior is bounded to listed async tests. |
| `tailtriage-tokio` | `tailtriage-tokio/examples/*.rs` | Tokio examples compile under tests | all features | `cargo test -p tailtriage-tokio --all-features --examples` | Minimal checkout and mini service integration compile against public API; both examples write `tailtriage-run.json` and print `cargo run -p tailtriage-cli -- analyze tailtriage-run.json --format json`. | `tailtriage-tokio/examples/minimal_checkout.rs`; `tailtriage-tokio/examples/mini_service_integration.rs` | secondary adoption proof | Runtime output values are not golden-asserted. |
| `demo-support` | `demos/demo_support/src/lib.rs` | inline demo support tests | all features | `cargo test -p demo-support --all-features --lib` | Demo CLI arg parsing, instrumentation modes, tracing shutdown writer behavior. | temp file/synthetic args | primary demo-support proof | Does not validate every service demo scenario. |
| `collector_stress` | `demos/collector_stress/src/main.rs` | inline CLI parsing tests | all features | `cargo test -p collector_stress --all-features --bin collector_stress` | Stress workload flags and legacy aliases parse/reject as expected. | synthetic args | secondary workload proof | Does not run stress workload or assert measurement values. |
| `runtime_cost` | `demos/runtime_cost/src/main.rs` | inline async/native tests and mode parsing | all features; current-thread async tests | `cargo test -p runtime_cost --all-features --bin runtime_cost` | Runtime-cost mode parsing, native finalize path, no-context baked-in path. | in-memory sink/synthetic modes | secondary empirical-workload proof | Does not run empirical measurements or validate overhead externally. |
| service demo binaries | `demos/{blocking_service,cold_start_burst_service,db_pool_saturation_service,downstream_service,executor_pressure_service,mixed_contention_service,queue_service,retry_storm_service,shared_state_lock_service}/src/main.rs` | binary test targets with zero tests | all features | `cargo test --workspace --all-features -- --list` | Binaries compile as test targets. | none at test-list time | secondary compile proof | Expected diagnosis is fixture/script/docs-backed for this inventory, not inline test-backed. |
| Python script tests | `scripts/tests/test_*.py` | repository-owned unittest modules | Python; platform depends on subprocess/temp paths | `.github/workflows/ci.yml` invokes dotted modules such as `python3 -m unittest scripts.tests.test_validate_docs_contracts`, `scripts.tests.test_check_diagnostic_fixture_integrity`, `scripts.tests.test_diagnostic_benchmark`, `scripts.tests.test_generate_diagnostic_scorecard`, `scripts.tests.test_measure_collector_limits`, `scripts.tests.test_measure_runtime_cost`, and `scripts.tests.test_validate_runtime_cost_summary`; file inspection also found unittest modules for demo scripts, diagnostic matrix, mitigation matrix, operational validation, validate-all, and collector-limit summary validation. | Validation scripts, demo script commands, scorecard/summary validators, fixture integrity command logic. | temp files and committed validation fixtures | secondary script-mechanism proof | This phase did not run the Python unit tests. |

## 3. Invariant catalog

| Invariant | Source owner | Primary proof owner | Secondary proof | Mechanism | Feature/platform | Evidence status |
|---|---|---|---|---|---|---|
| Public request/capture lifecycle uses one request context with queue/stage/inflight events and completion/finalization. | `tailtriage-core`, `tailtriage-tracing`, `tailtriage-tokio`, `tailtriage-controller` | `tailtriage-core/src/tests.rs` and `tailtriage-tracing/src/recorder.rs` tests | controller inline lifecycle/finalization tests; `tailtriage-analyzer/tests/end_to_end_capture_analysis.rs`; runtime-cost native finalize tests | inline synthetic lifecycle tests plus end-to-end integration | Tokio async where live tests apply | partial-to-complete for in-repo paths; external integration remains unknown. |
| Generic Run integrity and Run JSON schema accepts current committed fixture shapes. | `tailtriage-core` | `tailtriage-core/src/tests.rs` validation/schema tests | analyzer fixture tests, CLI JSON parity, and tracing equivalence expected Runs | serde parse/render fixture tests and core validation contracts | all features | complete for committed fixtures; absent for unknown external corpora. |
| Strict Run inspection rejects malformed/orphan request-scoped data while permissive normalization can clear or retain data with warnings. | `tailtriage-core` | `tailtriage-core/src/tests.rs` run-validation contract tests | analyzer strict/permissive tests; tracing JSONL tests; CLI boundary strict/non-strict tests | synthetic malformed Runs and JSONL import tests | all features | partial; searches found tests for representative classes, not an exhaustive malformed corpus. |
| Controller/capture finalization writes finalized Run JSON and reports lifecycle failures. | `tailtriage-controller`, `demos/demo_support`, `tailtriage-tracing` | `tailtriage-controller/src/lib.rs::tests` and demo-support shutdown writer tests | runtime-cost native finalize test; recorder async shutdown tests | temp-file/in-memory sink tests | Tokio async for live paths | partial; full controller process lifecycle outside unit tests is not separately golden-tested. |
| Tracing import records provenance and stable JSONL semantics, including strict/non-strict behavior. | `tailtriage-tracing` | `src/jsonl.rs` tests and `tests/equivalence.rs` | facade tracing tests | inline JSONL strings plus fixture-backed equivalence | all tracing features | duplicated primary/secondary proof for parsing and facade reachability; external version corpus absent. |
| Native and tracing-equivalent inputs produce stable expected Runs/reports for selected scenarios. | `tailtriage-tracing` | `tests/equivalence.rs` | `tests/equivalence_contract.rs` | committed fixtures and expected outputs | all features | complete for selected equivalence fixtures; scenario-generation rationale absent. |
| Analyzer option registry paths are unique, defaults match descriptors, TOML/CLI overrides converge, and invalid overrides are transactional. | `tailtriage-analyzer/src/options/*` | inline `options::overrides` and `options::registry` tests | CLI boundary config tests | exhaustive registry loops and synthetic overrides | all features | strong in-repo proof; historical rationale for 30 paths not established here. |
| Analyzer scoring/ranking/order uses deterministic tie-breaks and stable calibrated/final confidence behavior. | `tailtriage-analyzer` | inline analyzer/scoring tests | fixture expected report JSON | boundary/permutation tests and golden JSON | all features | strong for named scenarios; no proof of statistical causality is claimed. |
| Analyzer rendering produces stable compact/pretty JSON value equivalence and stable selected text reports. | `tailtriage-analyzer` | inline top-level rendering tests | CLI JSON parity; expected report fixtures | serde JSON equality/golden text assertions | all features | partial-to-strong; full text output is selectively tested, not every scenario. |
| CLI artifact naming, output routing, config parsing, strict/permissive flags, and overwrite policy are stable. | `tailtriage-cli` | `tests/cli_boundary.rs` and `src/artifact.rs` tests | `tests/json_parity.rs` | integration harness/temp files | all features | strong for tested policies; installed binary surface not separately inventoried. |
| Facade reexports compile under all features. | `tailtriage` | `tailtriage/src/lib.rs` tests and `tailtriage/tests/tracing_facade.rs` | example smoke scripts | compile-surface tests | all features | partial; non-default feature matrices unknown. |
| Serialized output is deterministic for analyzer fixtures, tracing equivalence expected outputs, validation lockfiles for analyzer-execution inputs, and scorecards where checked. | analyzer/tracing/scripts | fixture tests and `scripts/check_diagnostic_fixture_integrity.py` | scorecard generation tests | golden JSON, lock JSON, script tests | deterministic local file order assumed | partial; some latest scorecards are generated/manual and machine-scoped. |
| Safety/compatibility preserves legacy demo args and tracing duration-only legacy JSONL. | demo-support, collector-stress, tracing | demo-support/collector-stress parse tests; tracing equivalence fixture `duration_only_legacy.jsonl` | CLI boundaries | legacy fixture/arg parsing | all features | partial; external legacy population unknown. |
| Runtime sampling and Tokio helpers preserve configured sampling/helper behavior. | `tailtriage-tokio` | `tailtriage-tokio/src/lib.rs::{tests, helper_tests}` | controller sampler lifecycle tests; runtime-cost native tests | synthetic Tokio runtimes and helper futures | Tokio async | partial; production scheduler interleavings are not established by repository evidence. |
| Axum HTTP status outcome mapping stays stable. | `tailtriage-axum` | `tailtriage-axum/src/lib.rs::tests` | `tailtriage-axum/tests/axum_adapter.rs` | status mapping assertions and adapter integration | all features | partial; service-level load behavior is not established. |
| Runtime-cost and collector-limit outputs are workload/machine scoped rather than universal guarantees. | `demos/runtime_cost`, `demos/collector_stress`, validation scripts | summary validators and README/scorecard files | script tests | inspect committed scorecards and validation scripts | machine-sensitive | partial; empirical workloads not run in this phase. |

## 4. Fixture, golden, snapshot, and committed-data inventory

| Fixture family | Count / approx size | Consumer | Producer/generator | Durable contract purpose | Update/regeneration command | Drift-check owner | Determinism assumptions | Schema/version coupling | Overlap |
|---|---:|---|---|---|---|---|---|---|---|
| `tailtriage-analyzer/tests/fixtures/*.json` | 9 / ~20.9 KiB | `tailtriage-analyzer/tests/analyzer_fixtures.rs`; some CLI parity/smoke references | not established | Analyzer input Run corpus for queue, blocking, downstream, executor, mixed, scoped, insufficient evidence | not found | analyzer fixture tests | deterministic serde parse and sorted reports | Run schema + analyzer report schema | overlaps validation diagnostics corpus and demo fixtures semantically. |
| `tailtriage-analyzer/tests/expected/*.report.json` | 9 / ~32.9 KiB | analyzer fixture tests | generated by unknown/manual process | Golden analyzer report JSON | not found | analyzer fixture tests | stable rendering and ordering | report schema | overlaps CLI JSON parity and validation scorecards. |
| `tailtriage-tracing/tests/fixtures/equivalence/*.jsonl` plus `native_runs.json` | 5 / ~82.4 KiB | tracing equivalence tests | not found | Stable tracing import and native parity corpus | not found | equivalence tests/contract | stable JSONL event ordering | stable tracing JSONL + Run schema | overlaps analyzer fixture concepts for queue/route/temporal/truncation. |
| `tailtriage-tracing/tests/expected/equivalence/*.json` | 8 / ~48.8 KiB | tracing equivalence tests | not found | Expected Run/report output for tracing equivalence | not found | equivalence tests | stable analyzer and import rendering | Run and report schema | overlaps analyzer expected reports. |
| `tailtriage-tracing/tests/fixtures/tailtriage-span-v1.jsonl` | 1 / 765 B | tracing JSONL/API tests | not found | Stable span v1 JSONL fixture | not found | tracing tests | fixed JSONL lines | stable span schema | overlaps examples `tracing_spans.jsonl`. |
| `tailtriage-tracing/examples/tracing_spans.jsonl` | 1 file | tracing examples | committed example data | Example completed-span import data | not found | example tests inspect/run examples | fixed JSONL lines | stable span schema | overlaps tracing test fixture. |
| `demos/*/fixtures/*.json` service fixtures enumerated by `scripts/check_demo_fixture_drift.py::_scenario_specs()` | 23 enumerated paths within the service fixture directories | CLI demo smoke tests; demo docs/scripts | `scripts/check_demo_fixture_drift.py` regenerates matching generated outputs for `_scenario_specs()` | Sample/before/after/before-after analysis artifacts for enumerated service demos | `python3 scripts/check_demo_fixture_drift.py --profile dev --refresh` | `python3 scripts/check_demo_fixture_drift.py --profile dev` | deterministic demo generation for the selected profile | analyzer report schema | overlaps validation mitigation matrix and analyzer scenario fixtures. |
| `validation/diagnostics/corpus/*.json` and `*.jsonl` | 22 / ~82.8 KiB | diagnostic matrix scripts; integrity script only for manifest `analyzer_execution` `run_artifact`/`tracing_span_jsonl` entries | committed corpus, producer unknown | Deterministic diagnostic validation corpus including partial/truncated/weak/missing evidence | `python3 scripts/run_diagnostic_matrix.py` consumes; generation source is not established by repository evidence | `scripts/check_diagnostic_fixture_integrity.py` for analyzer-execution raw artifacts only; script tests | fixed corpus inputs; expected output generated into scorecards/locks | Run schema, JSONL schema, analyzer behavior | report-contract corpus cases are not protected by the analyzer fixture integrity lock. |
| `validation/diagnostics/corpus/run-artifacts/*.json` | 5 / ~10.8 KiB | diagnostic matrix run-artifact analyzer-execution cases | unknown | Alternate run-artifact corpus for missing instrumentation variants | same diagnostic scripts | `scripts/check_diagnostic_fixture_integrity.py` protects these files when listed as analyzer-execution raw artifacts | fixed inputs | Run schema | overlaps nearby corpus files with same scenario names. |
| `validation/diagnostics/manifest.json` | 1 file | diagnostic matrix/generator scripts | committed manifest | Scenario registry and expected diagnostic metadata | not established beyond scripts | script tests | deterministic scenario registry | validation schema | overlaps corpus file identities. |
| `validation/diagnostics/analyzer-fixtures.lock.json` | 1 / included in ~66.6 KiB diagnostics root | integrity script for manifest cases with `validation_class = "analyzer_execution"` | `scripts/check_diagnostic_fixture_integrity.py --refresh` rewrites only this lock | Lock for diagnostics manifest analyzer-execution raw `run_artifact` and `tracing_span_jsonl` inputs | `python3 scripts/check_diagnostic_fixture_integrity.py --refresh` | `python3 scripts/check_diagnostic_fixture_integrity.py` | fixed hashes/metadata/shapes for analyzer-execution inputs | diagnostics manifest artifact schema | does not protect report-contract cases or analyzer expected report JSON. |
| `validation/diagnostics/latest/scorecard.md` | 1 / ~4 KiB | humans and validation docs | `scripts/generate_diagnostic_scorecard.py` | Latest diagnostic validation scorecard | diagnostic scorecard script | script tests | deterministic over input summaries, but latest run status may be generated | validation manifest/results | overlaps public validation docs. |
| `validation/runtime-cost/latest/scorecard.md` | 1 / 140 B; plus README | runtime-cost validators | `scripts/measure_runtime_cost.py` and `validate_runtime_cost_summary.py` | Machine/workload-scoped runtime-cost summary | runtime-cost measurement script | summary validator/tests | machine-sensitive measurements | runtime-cost summary schema | overlaps runtime_cost demo/workload. |
| `validation/collector-limits/latest/scorecard.md` | 1 / 164 B; plus README | collector-limit validators | `scripts/measure_collector_limits.py` / stress measurement scripts | Collector-limit bounded-drop/warning summary | collector-limit measurement scripts | summary validator/tests | machine-sensitive/stress-sensitive | collector-limit summary schema | overlaps collector_stress workload. |
| `examples/analyzer-config.toml` | 1 / ~1.5 KiB | analyzer TOML tests and user examples | manual committed example | Canonical v1 analyzer config example | not found | analyzer TOML tests | stable TOML parsing | analyzer option schema | overlaps CLI config policy tests. |
| `Cargo.toml`, member `Cargo.toml`, `deny.toml` | 21 manifests plus deny config | Cargo, cargo-deny if used | manual | Package graph, feature and dependency lock policy | Cargo commands | CI/workflow if invoked | deterministic metadata with lockfile | Cargo schema | overlaps 23A-1 package inventory. |

No `.snap` files were found by the targeted file search.

## 5. Examples, demos, and workload inventory

| Package | Entrypoint | Classification | Scenario identity | Normal invocation | Expected behavior/evidence | Proof owner | Assertion status | Generated artifacts | Sensitivity | Mitigation pairing / overlap |
|---|---|---|---|---|---|---|---|---|---|---|
| `tailtriage-tokio` | `tailtriage-tokio/examples/minimal_checkout.rs` | minimal adoption example | minimal checkout/request instrumentation | `cargo run -p tailtriage-tokio --example minimal_checkout` | Demonstrates basic Tokio request capture; source writes `tailtriage-run.json` and prints `cargo run -p tailtriage-cli -- analyze tailtriage-run.json --format json`. | example compile/tests/scripts | compile/example smoke; exact output values not golden here | `tailtriage-run.json` | low timing sensitivity | overlaps README/user-guide adoption path. |
| `tailtriage-tokio` | `tailtriage-tokio/examples/mini_service_integration.rs` | minimal adoption example | mini service integration | `cargo run -p tailtriage-tokio --example mini_service_integration` | Demonstrates service-style request context usage; source writes `tailtriage-run.json` and prints `cargo run -p tailtriage-cli -- analyze tailtriage-run.json --format json`. | example compile/tests/scripts | compile/smoke; exact output values not golden here | `tailtriage-run.json` | async sleeps are present in source | overlaps Axum adoption examples. |
| `tailtriage-axum` | `tailtriage-axum/examples/axum_core_manual.rs` | minimal adoption example | manual Axum/core integration | `cargo run -p tailtriage-axum --example axum_core_manual` | Shows Axum integration without framework rewrite. | Axum tests/example smoke | compile/smoke | stdout/artifact behavior is not established by repository evidence | network/runtime sensitivity if serving | overlaps `axum_service_adoption`. |
| `tailtriage-axum` | `tailtriage-axum/examples/axum_service_adoption.rs` | minimal adoption example | Axum service adoption | `cargo run -p tailtriage-axum --example axum_service_adoption` | Shows service adoption path. | Axum adapter tests/scripts | compile/smoke | stdout/artifact behavior is not established by repository evidence | async/timing sensitivity | overlaps Tokio mini service example. |
| `tailtriage-controller` | `tailtriage-controller/examples/controller_minimal.rs` | minimal adoption example | controller minimal startup | `cargo run -p tailtriage-controller --example controller_minimal` | Demonstrates controller setup/finalization. | controller example compile/smoke script | compile/smoke | controller artifact behavior is not established by repository evidence | timing/filesystem sensitivity | overlaps controller TOML example. |
| `tailtriage-controller` | `tailtriage-controller/examples/controller_toml_startup.rs` | minimal adoption example | controller TOML startup | `cargo run -p tailtriage-controller --example controller_toml_startup` | Demonstrates TOML-driven controller startup. | smoke controller script | compile/smoke | controller artifact behavior is not established by repository evidence | filesystem/config sensitivity | overlaps `examples/analyzer-config.toml` config story. |
| `tailtriage-tracing` | `examples/completed_span_jsonl_export.rs` | minimal adoption/example-based proof | completed span JSONL export | `cargo run -p tailtriage-tracing --example completed_span_jsonl_export` | Emits/imports completed span JSONL. | `tests/tracing_examples.rs` | partially asserted by example tests | JSONL output | low timing sensitivity | overlaps `examples/tracing_spans.jsonl`. |
| `tailtriage-tracing` | `examples/live_session_snapshot.rs` | minimal adoption/example-based proof | live session snapshot | `cargo run -p tailtriage-tracing --example live_session_snapshot` | Demonstrates live session snapshot. | tracing example/live API tests | compile/run smoke | Run/snapshot output | async timing sensitivity | overlaps live API tests. |
| `tailtriage-tracing` | `examples/live_session_to_run.rs` | minimal adoption/example-based proof | live session to Run | `cargo run -p tailtriage-tracing --example live_session_to_run` | Demonstrates shutdown/finalize to Run. | tracing example/live API tests | compile/run smoke | Run JSON/stdout | async timing sensitivity | overlaps recorder tests. |
| `tailtriage-tracing` | `examples/tokio_session_to_run.rs` | minimal adoption/example-based proof | Tokio runtime session to Run | `cargo run -p tailtriage-tracing --example tokio_session_to_run` | Demonstrates runtime sampling conversion. | tracing example/tokio_session tests | compile/run smoke | Run JSON/stdout | Tokio runtime metrics/timing | overlaps runtime sampling tests. |
| `blocking-service-demo` | `demos/blocking_service/src/main.rs` | diagnostic demonstration | blocking-pool pressure service | `cargo run -p blocking-service-demo -- ...` or demo scripts | Expected blocking-pool suspect in sample/before fixtures; before/after comparison committed. | demo smoke fixture tests/scripts | expected output asserted indirectly from fixtures, not binary execution here | analysis JSON fixtures | service timing/load sensitivity | before/after pairing; overlaps validation blocking-correlated-stage. |
| `queue-service-demo` | `demos/queue_service/src/main.rs` | diagnostic demonstration | application queueing service | `cargo run -p queue-service-demo -- ...` | Expected queueing suspect in fixtures. | demo smoke tests/scripts | fixture assertions | sample/before/after analysis JSON | timing/load sensitivity | before/after; overlaps analyzer `queue_saturation`. |
| `executor-pressure-service-demo` | `demos/executor_pressure_service/src/main.rs` | diagnostic demonstration | executor pressure | `cargo run -p executor-pressure-service-demo -- ...` | Expected executor-pressure suspect in fixtures. | demo smoke tests/scripts | fixture assertions | sample/before/after/comparison JSON | runtime scheduling sensitivity | before/after; overlaps analyzer `executor_pressure`. |
| `downstream_service` | `demos/downstream_service/src/main.rs` | diagnostic demonstration | slow downstream stage | `cargo run -p downstream_service -- ...` | Expected downstream-stage suspect in fixtures. | demo smoke tests/scripts | fixture assertions | sample/before/after/comparison JSON | timing sensitivity | before/after; overlaps analyzer `downstream_stage`. |
| `db-pool-saturation-service-demo` | `demos/db_pool_saturation_service/src/main.rs` | diagnostic demonstration | DB pool saturation | `cargo run -p db-pool-saturation-service-demo -- ...` | Expected queue/downstream-like contention evidence from fixture naming; exact diagnosis from fixture content not exhaustively restated. | demo smoke tests/scripts | fixture assertions | before/after/comparison JSON | timing/load sensitivity | before/after; overlaps queue/downstream scenarios. |
| `retry-storm-service-demo` | `demos/retry_storm_service/src/main.rs` | diagnostic demonstration | retry storm | `cargo run -p retry-storm-service-demo -- ...` | Expected changed analysis before/after in committed fixtures. | demo smoke tests/scripts | fixture assertions | before/after/comparison JSON | timing/load sensitivity | before/after; overlaps downstream/queue ambiguity. |
| `shared-state-lock-service-demo` | `demos/shared_state_lock_service/src/main.rs` | diagnostic demonstration | shared-state lock contention | `cargo run -p shared-state-lock-service-demo -- ...` | Expected contention evidence in fixtures. | demo smoke tests/scripts | fixture assertions | before/after/comparison JSON | timing/scheduler sensitivity | before/after; overlaps blocking/queue contention. |
| `cold-start-burst-service-demo` | `demos/cold_start_burst_service/src/main.rs` | diagnostic demonstration | cold-start burst | `cargo run -p cold-start-burst-service-demo -- ...` | Expected before/after analysis for burst behavior. | demo smoke tests/scripts | fixture assertions | before/after/comparison JSON | timing/startup sensitivity | before/after; overlaps temporal validation. |
| `mixed_contention_service` | `demos/mixed_contention_service/src/main.rs` | diagnostic demonstration | mixed contention baseline/mitigated | `cargo run -p mixed_contention_service -- ...` | Expected mixed suspects and mitigated comparison in fixtures. | demo smoke tests/scripts | fixture assertions | baseline/mitigated/comparison JSON | timing/load sensitivity | explicit baseline/mitigated; overlaps analyzer mixed fixtures. |
| `collector_stress` | `demos/collector_stress/src/main.rs` | stress workload | collector limits under high request/event volume | `cargo run -p collector_stress -- ...` or measurement scripts | Exercises collector cap/drop/warning/downgrade behavior; not ordinary diagnostic demo. | inline parse tests; collector measurement/summary scripts | CLI parsing asserted; stress output not run here | collector-limit summaries under validation when measured | machine/load sensitive | overlaps validation/collector-limits. |
| `runtime_cost` | `demos/runtime_cost/src/main.rs` | empirical measurement | runtime overhead modes: baked-in/native/tracing variants | `cargo run -p runtime_cost -- ...` or `python3 scripts/measure_runtime_cost.py` | Measures machine/workload-scoped runtime cost; not universal guarantee. | inline mode/finalize tests; runtime-cost scripts | parser/finalize asserted; measurement not run here | runtime-cost summaries under validation | strongly machine/timing sensitive | overlaps validation/runtime-cost. |
| `demo-support` | `demos/demo_support/src/lib.rs` | shared support package | shared demo CLI/outcome/instrumentation/tracing writer | used by demo binaries | Provides common arg parsing and writer behavior. | inline tests | asserted | run JSON files via writer | filesystem/timing for writer | shared by multiple demos. |

## 6. Validation-mechanism inventory

| Mechanism | Language/framework | Local invocation | Inputs | Outputs/generated files | Failure condition | Determinism | Owner | References |
|---|---|---|---|---|---|---|---|---|
| Cargo all-feature test listing | Cargo/Rust | `cargo test --workspace --all-features -- --list` | workspace manifests/source | compiled test target list | compile failure or list failure | deterministic names, compile environment-sensitive | Cargo workspace | used in this phase; CI uses Cargo commands in workflows. |
| Rust formatting | Rustfmt | `cargo fmt --check` | Rust source | none | formatting drift | deterministic | workspace | AGENTS/CI style requirement. |
| Analyzer fixture tests | Rust integration tests | `cargo test -p tailtriage-analyzer --test analyzer_fixtures` | analyzer fixture and expected report JSON | none | report mismatch | deterministic | analyzer crate | Cargo test target. |
| Tracing equivalence tests | Rust integration tests | `cargo test -p tailtriage-tracing --test equivalence --all-features` | tracing equivalence fixtures/expected | none | run/report mismatch | deterministic | tracing crate | Cargo test target. |
| CLI demo smoke fixtures | Rust integration tests | `cargo test -p tailtriage-cli --test demo_smoke_fixtures --all-features` | demo fixture JSON | none | missing/invalid/unexpected fixture properties | deterministic over committed files | CLI crate | Cargo test target. |
| Python script tests | Python unittest | dotted `python3 -m unittest scripts.tests.test_...` modules; CI invokes selected modules and file inspection found 13 `scripts/tests/test_*.py` modules | script temp fixtures and committed validation files | temp outputs | assertion failure | deterministic except subprocess/platform dependencies | scripts | repository-owned unittest modules; not run here. |
| Docs contract validator | Python script | `python3 scripts/validate_docs_contracts.py` | Markdown files/docs index | none | required docs links/wording missing | deterministic over checkout | docs contract | AGENTS and script tests; run here and expected to fail for review files. |
| Diagnostic fixture integrity | Python script | `python3 scripts/check_diagnostic_fixture_integrity.py` | manifest cases with `validation_class = "analyzer_execution"`; their raw `run_artifact` and `tracing_span_jsonl` files; `validation/diagnostics/analyzer-fixtures.lock.json` | drift check for the analyzer fixture lock; regeneration uses `python3 scripts/check_diagnostic_fixture_integrity.py --refresh` and rewrites only `validation/diagnostics/analyzer-fixtures.lock.json` | hash/manifest/shape mismatch for analyzer-execution artifacts | deterministic | validation diagnostics analyzer-execution lock | script tests and validation docs; does not protect report-contract cases or `tailtriage-analyzer/tests/expected/*.report.json`. |
| Diagnostic matrix | Python script | `python3 scripts/run_diagnostic_matrix.py` | `validation/diagnostics/manifest.json` and corpus | diagnostic results/summary (path depends on script args) | scenario expectation mismatch | deterministic over analyzer version and fixtures | validation diagnostics | script tests and validation docs. |
| Mitigation matrix | Python script | `python3 scripts/run_mitigation_matrix.py` | demo before/after fixture families | mitigation summary | expected movement mismatch | deterministic over fixtures | validation/demo scripts | script tests. |
| Diagnostic scorecard generation | Python script | `python3 scripts/generate_diagnostic_scorecard.py` | diagnostic results/manifest | `validation/diagnostics/latest/scorecard.md` when invoked that way | missing metrics/status mismatch | deterministic over inputs | validation diagnostics | script tests. |
| Runtime-cost measurement | Python script + Rust workload | `python3 scripts/measure_runtime_cost.py` | `demos/runtime_cost` binary/modes | runtime-cost summary/scorecard | measurement command failure or invalid summary | machine-sensitive | runtime-cost validation | script tests and validators. |
| Runtime-cost summary validator | Python script | `python3 scripts/validate_runtime_cost_summary.py` | runtime-cost summary/scorecard | none | invalid/missing fields | deterministic over file | runtime-cost validation | script tests. |
| Collector-limit measurement | Python script + Rust stress workload | `python3 scripts/measure_collector_limits.py` and `scripts/measure_collector_stress.py` | `demos/collector_stress` | collector-limit summaries/scorecards | command failure or invalid expected warning/drop behavior | machine/load-sensitive | collector-limits validation | script tests. |
| Collector-limit summary validator | Python script | `python3 scripts/validate_collector_limits_summary.py` | collector-limit summary/scorecard | none | invalid/missing fields | deterministic over file | collector-limits validation | script tests. |
| Operational validation | Python script | `python3 scripts/run_operational_validation.py` | demo/service commands and configs | operational validation artifacts | command/expectation failure | mixed deterministic and runtime-sensitive | validation scripts | script tests. |
| Demo fixture drift check | Python script | `python3 scripts/check_demo_fixture_drift.py --profile dev` | demo fixtures enumerated by `_scenario_specs()` and demo scripts | drift report; regeneration uses `python3 scripts/check_demo_fixture_drift.py --profile dev --refresh` | fixture mismatch | deterministic for selected profile if demo outputs are stable | demo scripts | script name, `_scenario_specs()`, and tests references. |
| Smoke public examples/external consumer/controller | Python scripts | `python3 scripts/smoke_public_examples.py`; `python3 scripts/smoke_external_consumer.py`; `python3 scripts/smoke_controller_example.py` | examples and temporary external crates | temp build dirs/artifacts | compile/run failure | environment/network/toolchain sensitive | scripts/examples | script references and tests. |
| Workflow references | YAML | `.github/workflows/ci.yml`; `.github/workflows/validation-snapshot.yml` | Cargo/Python validation commands | CI logs/artifacts | workflow command failure | CI-machine sensitive | GitHub workflows | inspected only for invocation existence, not cadence. |

## 7. Generated-output ownership

- Analyzer expected report JSON under `tailtriage-analyzer/tests/expected/` is owned by analyzer fixture tests; no generator command was found by targeted searches.
- Tracing equivalence expected Run/report JSON under `tailtriage-tracing/tests/expected/equivalence/` is owned by tracing equivalence tests; no generator command was found.
- Demo fixture paths enumerated by `scripts/check_demo_fixture_drift.py::_scenario_specs()` are drift-checked by `python3 scripts/check_demo_fixture_drift.py --profile dev` and regenerated by `python3 scripts/check_demo_fixture_drift.py --profile dev --refresh`; demo fixtures outside `_scenario_specs()` are not established as owned by that script.
- `validation/diagnostics/latest/scorecard.md`, `validation/runtime-cost/latest/scorecard.md`, and `validation/collector-limits/latest/scorecard.md` are latest generated/summary artifacts owned by validation scripts; runtime-cost and collector-limit outputs are machine/workload/profile scoped.
- `validation/diagnostics/analyzer-fixtures.lock.json` is the generated integrity lock for diagnostics manifest cases with `validation_class = "analyzer_execution"`; the protected inputs are those cases' raw `run_artifact` and `tracing_span_jsonl` files plus the lock itself. `python3 scripts/check_diagnostic_fixture_integrity.py` is its drift check, and `python3 scripts/check_diagnostic_fixture_integrity.py --refresh` rewrites only that lock.

## 8. Repeated and parallel proof surfaces

- OBS-23A2-001: `tailtriage-analyzer/tests/fixtures/queue_saturation.json`, `validation/diagnostics/corpus/*queue*`, and `demos/queue_service/fixtures/*.json` share queue-dominant semantics. They differ by owner: analyzer unit/golden fixture, validation corpus, and service demo artifact. Tests establish each is consumed; repository evidence does not establish whether they were generated from a single source.
- OBS-23A2-002: `tailtriage-analyzer/tests/fixtures/downstream_stage.json`, `validation/diagnostics/corpus/weak-blocking-strong-downstream.json`, and `demos/downstream_service/fixtures/*.json` repeat downstream-stage proof surfaces. They differ in strength/ambiguity and before-after service framing. No reason for all variants beyond coverage breadth is documented in tests.
- OBS-23A2-003: Strict/permissive malformed input behavior is present in analyzer inline tests, `tailtriage-tracing/src/jsonl.rs` tests, and `tailtriage-cli/tests/cli_boundary.rs`. Shared invariant is reject-or-warn policy; differences are Run inspection, JSONL import, and CLI flag behavior. Repository evidence establishes layering, not completeness across all malformed classes.
- OBS-23A2-004: Analyzer rendering stability is covered by inline render tests, analyzer expected report JSON, CLI JSON parity tests, and tracing equivalence expected reports. Shared contract is stable JSON/report values; differences are entrypoint and input family. Evidence does not identify one canonical golden owner.
- OBS-23A2-005: Facade compile surfaces are present in `tailtriage/src/lib.rs`, `tailtriage/tests/tracing_facade.rs`, examples, and smoke scripts. Shared behavior is public reachability; differences are all-feature in-crate tests versus external/consumer smoke. Evidence cannot establish downstream non-default feature use.
- OBS-23A2-006: Runtime sampling/finalization is exercised by tracing recorder tests, tracing Tokio session tests, runtime-cost native finalize tests, and controller/demo support paths. Shared invariant is finalized Run availability; differences are synthetic live session, runtime sampling, empirical workload, and writer. Evidence does not establish all production lifecycle interleavings.
- OBS-23A2-007: Before/after mitigation is present in multiple demo fixture families and `scripts/run_mitigation_matrix.py`. Shared pattern is comparison artifact movement; differences are service scenario and suspect class. Tests/scripts establish comparison consumption, not formal causal proof.
- OBS-23A2-008: Validation scorecards and public validation docs reference deterministic, runtime-cost, and collector-limit validation. Shared content is latest validation evidence; differences are machine-sensitive versus deterministic status. Evidence does not establish universal production performance guarantees.

## 9. Evidence gaps

- No generator command was found for analyzer expected reports, tracing equivalence expected reports, report-contract cases, or demo fixtures outside `scripts/check_demo_fixture_drift.py::_scenario_specs()`.
- Service demo binaries compile as workspace test targets but reported zero inline tests in the unmasked workspace test listing; expected diagnoses are asserted through committed fixture consumers and scripts rather than live binary runs in this inventory.
- The population and schema versions of external persisted Run/JSONL artifacts remain unknown.
- Non-default facade/tracing feature combinations used by external consumers are not established by repository tests.
- Runtime-cost and collector-limit evidence is intentionally machine/workload/profile scoped; this phase did not run empirical or stress workloads.
- Some repeated fixtures share scenario names or semantics, but repository evidence does not establish a single source of truth or generation lineage across analyzer, tracing, validation, and demo families.
- Documentation contract validation fails on temporary review Markdown files by design for this evidence branch.

## 10. Mechanical observations

- The workspace contains 20 packages in Cargo metadata: 8 product crates, `demo-support`, and 11 demo/workload binaries.
- `cargo test --workspace --all-features -- --list > /tmp/23a2-test-list-rerun.txt 2>&1` exited 0 and listed Rust tests across core, controller, Tokio, analyzer, tracing, CLI, facade, Axum, demo-support, collector_stress, and runtime_cost, while several service demo binaries reported `0 tests, 0 benchmarks`.
- Targeted file discovery found no `.snap` files.
- Fixture families are concentrated in analyzer tests, tracing equivalence tests, demo service fixture directories, and `validation/diagnostics`.
- Empirical and stress surfaces are present as separate workloads: `demos/runtime_cost` and `demos/collector_stress`, with validation directories and scripts separate from ordinary diagnostic demos.

## 11. Command summary

- `git status --short` and `git rev-parse HEAD`: clean tree and expected SHA verified.
- `cargo metadata --format-version 1 --locked`: succeeded and established workspace/package/target inventory.
- `cargo test --workspace --all-features -- --list > /tmp/23a2-test-list-rerun.txt 2>&1`: exited 0 after compilation and established test target/list evidence without running test bodies.
- Focused `find`, `rg`, and Python size summaries: succeeded and established test/fixture/demo/validation locations and approximate sizes.
- `python3 scripts/validate_docs_contracts.py`: expected failure because temporary review Markdown files are not linked from public docs index.
