# Validation

## Summary
`tailtriage` validation checks whether evidence-ranked suspects are useful triage leads under controlled Tokio workloads. It is a triage tool, not a root-cause proof engine.

## Scope of validation
Validation is machine-scoped and workload-scoped. Results in this repository are deterministic fixture/corpus checks, not universal production guarantees.

## Claims validated
Under controlled and documented Tokio service workloads, tailtriage reliably classifies the dominant bottleneck family, avoids high-confidence claims when evidence is weak or mixed, exposes truncation/partial-data limits, and provides useful next checks.

## Claims not validated
- universal production overhead
- root-cause proof from one run
- replacement for tracing, metrics, tokio-console, or production observability
- correct diagnosis when critical instrumentation is missing
- precise blocking-vs-executor separation on stable Tokio in all cases

## Methodology
Validation corpus cases map analysis reports to independent labels and acceptance rules: top-1/top-2 classification, required evidence cues, and allowed warning substrings.

## Diagnostic matrix
See `validation/diagnostics/latest/scorecard.md` and `validation/diagnostics/manifest.json`.

## Scenario results
See latest benchmark metrics generated by `scripts/diagnostic_benchmark.py`.

## Overhead validation
Runtime-overhead validation is measured separately in `docs/runtime-cost.md`.

## Collector-limit validation
Collector-limit and truncation behavior is measured separately in `docs/collector-limits.md`.

## Reproducibility
Corpus and benchmark are deterministic and fixture-backed.

## CI coverage
- `python3 scripts/diagnostic_benchmark.py --manifest validation/diagnostics/manifest.json`
- `python3 -m unittest scripts.tests.test_diagnostic_benchmark`

## Known limitations
- score is an evidence-ranking score, not probability
- confidence is score-derived, not causal certainty
- suspects are evidence-ranked leads, not proof

## Future validation work
- repeated-run stability validation
- perturbation and sensitivity validation
- expanded real-service corpus
